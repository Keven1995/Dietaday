services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dietapp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - dietapp_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dietapp"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  dietapp_postgres:
